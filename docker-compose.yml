version: "3.7"

services:
    rf-fe-dev:
        container_name: rf-fe-dev
        build:
            context: .
            dockerfile: Dockerfile
        volumes:
            - .:/app
        restart: unless-stopped
        networks:
            - proxy-bridge

networks:
    proxy-bridge:
        external: true
        name: proxy-bridge
