version: "3.7"
services:
    rf-fe-landingpage-test:
        build:
            context: .
            dockerfile: Dockerfile.test
        container_name: rf-fe-test
        restart: unless-stopped
        volumes:
            - ./nginx:/nginx
        networks:
            - proxy-bridge
networks:
    proxy-bridge:
        external: true
        name: proxy-bridge
